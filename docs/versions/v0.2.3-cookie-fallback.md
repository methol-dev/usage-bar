---
version: v0.2.3
codename: cookie-fallback
status: wontfix
target_date: null
shipped_date: null
includes_specs: []
release_notes_zh: |
  v0.2.3 — cookie-fallback：**永久不实施**（scope-cut）

  作者决策：不引入浏览器 cookie 读取作为 OAuth 回退路径。

  评估理由（2026-05-11 user 明确决策）：
  - Safari `~/Library/Cookies/Cookies.binarycookies` 需 Full Disk Access；触发 macOS TCC 提示
  - Chrome 系 Cookies SQLite 含 OS-level 加密；需 Keychain 提取 master key
  - Firefox Cookies SQLite + sessionStore 可能锁定（浏览器运行中）
  - 第三方浏览器 ToS 可能限制非 UA 进程访问其 cookie 存储
  - 与 v0.1.1 Claude CLI Keychain 凭证 + v0.1.2 本地 JSONL + v0.1.3 multi-account 三路径相比，cookie fallback 收益边际且引入大量隐私 / 合规风险面

  现有数据源覆盖度已充分：
  - 主路径：OAuth API（v0.1.0~）
  - 凭证 bootstrap：Claude CLI Keychain（v0.1.1）
  - 离线 cost：本地 JSONL（v0.1.2）
  - 多账号：accounts store（v0.1.3）

  如未来需补充 OAuth 之外的数据源，优先考虑 v0.2.4 CLI PTY 兜底（不读浏览器 cookie，只调用用户已安装的 Claude CLI）。
---

# v0.2.3 — cookie-fallback（wontfix / scope-cut）

> ⚠️ **永久不实施**（user 决策 2026-05-11）。本文件作为决策记录保留，不再升 placeholder→planned。

## 决策

不引入浏览器 cookie 读取路径。详细理由见 frontmatter `release_notes_zh`。

## 路线影响

- v0.2.3 版本号**保留**但 status 永久 `wontfix`，**不打 git tag**
- v0.2.4 cli-pty-fallback 顺延为 Phase 2 最后的 OAuth 兜底候选（不涉及浏览器 cookie，仅 PTY 调用用户已装 CLI）
- v0.2.5 widgetkit / v0.2.6 cli-tool / v0.3.0 phase3-milestone 路线不变

## 引用

- 路线：[`README.md`](./README.md)
- 决策对话：2026-05-11 user 明确 "不要 cookie fallback，跳过这个需求"
- 替代路径：[`v0.2.4-cli-pty-fallback.md`](./v0.2.4-cli-pty-fallback.md)
